let randomNumber = Math.random(); // produces random number between 0 (including) and 1 (excluding)


function randoming(randomNumber){
    // let i = 0;
    while(randomNumber > 0.7){
        alert('The while loop is working');
        // i ++;
        randomNumber++;
    }
}

randoming();

